# =========================================================
# MoodTune ML Service — Python Requirements
# Install: pip install -r requirements.txt
# =========================================================

# Web server
flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0

# ── Emotion (FER-2013 training + TFLite inference) ────────
tensorflow==2.15.0
numpy==1.26.0
opencv-python-headless==4.9.0.80
matplotlib==3.8.2
seaborn==0.13.0
scikit-learn==1.4.0
yt_dlp==2026.3.17
# ── Voice analysis ────────────────────────────────────────
librosa==0.10.1
soundfile==0.12.1

# ── Stem separation (choose one or both) ─────────────────
audio-separator==0.17.0
# spleeter==2.4.0           # alternative / fallback

# ── ASR lyrics (Whisper) ─────────────────────────────────
openai-whisper==20231117
# Whisper also needs: ffmpeg installed on system
# Ubuntu: sudo apt install ffmpeg
# macOS:  brew install ffmpeg

# Utilities
python-dotenv==1.0.0
requests==2.31.0